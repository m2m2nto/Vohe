import Foundation

// Copy this file to BackendDefaults.swift and fill in your own value:
//
//     cp Vohe/Services/BackendDefaults.swift.template Vohe/Services/BackendDefaults.swift
//
// The copy is gitignored. Leaving the address empty is fine: the app then
// starts with no server and works entirely on-device.

/// Local-only default, so a fresh install doesn't have to type the server
/// address on a phone keyboard. There is nothing else to prefill — the app
/// signs in with a username and a password, and keeps only the session token
/// the server returns. Anything saved in Server settings wins from then on.
enum BackendDefaults {
    /// e.g. "https://your-dictionaries.vercel.app"
    static let address = ""
}
